import {
    Meta,
    Story,
    Canvas,
    ArgsTable,
    Title,
    Description
} from '@storybook/addon-docs/blocks';
import Table from './';
import Centered from '../../../stories/assets/Centered';

<Meta
    title='Components/Table'
    component={Table}
    argTypes={{
        headers: {
            control: 'disable'
        },
        inputData: {
            control: 'disable'
        }
    }}
/>

<Title>Table</Title>

<Description of={Table}>Table with sorting capability</Description>

<Canvas>
    <Story
        name='Table'
        args={{
            inputData: [
                {
                    firstName: 'Matti',
                    lastName: 'Meikäläinen',
                    id: 2
                },
                {
                    firstName: 'Esko',
                    lastName: 'Eerikkilä',
                    id: 1
                }
            ],
            headers: [
                { label: 'First name', key: 'firstName' },
                { label: 'Last name', key: 'lastName' },
                { label: 'ID', key: 'id' }
            ]
        }}
        parameters={{
            jest: ['Table']
        }}
    >
        {(args) => <Table {...args} />}
    </Story>
</Canvas>

<ArgsTable story='Table' />
